# GitHub Pages supplies both values from its workflow. For Vercel, leave the base path empty
# and set the site URL to the final https:// domain in Vercel's environment settings.
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_URL=
